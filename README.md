
  # Buat aplikasi dan web (Copy) (Copy)

  This is a code bundle for Buat aplikasi dan web (Copy) (Copy). The original project is available at https://www.figma.com/design/ErYhlcgba3BkYvVCaabTeL/Buat-aplikasi-dan-web--Copy---Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  